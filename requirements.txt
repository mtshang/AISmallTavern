# Direct runtime dependencies, pinned to the current Python 3.12 project environment.
# pip resolves and installs their transitive dependencies automatically.
openai==3.8.0
prompt-toolkit==3.0.53
python-dotenv==1.2.3
